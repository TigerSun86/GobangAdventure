namespace GobangGameLib.GameBoard.Patterns
{
    /// <summary>
    /// Defines whether the pattern is at the head, tail, or any position of a line.
    /// </summary>
    public enum PatternPositionType
    {
        Any,
        Head,
        Tail
    }
}
